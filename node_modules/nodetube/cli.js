const print = console.log;

const { Youtube } = require(".");

(async () => {
  const yt = new Youtube(process.argv[2]);
  const link = await yt.stream(251);
  print(link);
})();

// USAGE node cli.js https://youtube.com/watch?v=videoId
